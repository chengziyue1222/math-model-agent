# /prompt-only

## 用途
只生成prompt不执行，可保存为文件

## 用法
```bash
/prompt-only "你的想法"
```

## 功能说明
- 生成结构化prompt
- 不执行
- 保存为文件
- 便于后续使用

## 参数
- `input`: 非正式输入

## 示例
```bash
/prompt-only "设计一个实验方案"
/prompt-only "写一个文献综述"
```

## 输出
生成prompt文件
