# Math Model Agent 数学建模使用说明

> 当前版本：v7.2（2026-08-10）
>
> 推荐入口：仓库 `skills/` 下的 8 个标准 Codex Skills
>
> 能力范围：题意拆解、数据诊断、文献核验、模型选择、算法求解、质量验证、正式图表、论文生成、排版与对抗审查

v7.2 将此前分散的旧版命令收敛为一条证据驱动工作流。当前算法库包含 **25 个模块文件、144 个公开导出**；标准工作流提供执行前契约预检、可恢复阶段门、结果追溯和论文质量检查。它新增 `rapid`、`competition`、`audit` 三档工作流：先让建模洞察跑起来，再在交付风险需要时增加可复现与审计控制。CUMCM 排版、哈希绑定与独立复核不再被误当成所有任务的默认负担。

`legacy/skills/` 中的旧版 slash commands 仅用于历史兼容。新项目不要再以 `/mm-*`、`/nature-*` 或外部 `~/ai-skills/` 目录作为主入口。

---

## 目录

1. [快速开始](#1-快速开始)
2. [系统架构](#2-系统架构)
3. [8 个标准 Skills](#3-8-个标准-skills)
4. [标准项目目录](#4-标准项目目录)
5. [完整工作流](#5-完整工作流)
6. [运行契约与证据门禁](#6-运行契约与证据门禁)
7. [算法库](#7-算法库)
8. [功能库](#8-功能库)
9. [图表生成](#9-图表生成)
10. [论文生成与格式交付](#10-论文生成与格式交付)
11. [论文审查](#11-论文审查)
12. [测试与基准](#12-测试与基准)
13. [2021 C 题适配器边界](#13-2021-c-题适配器边界)
14. [常见问题](#14-常见问题)
15. [旧版资源说明](#15-旧版资源说明)

---

## 1. 快速开始

### 1.1 在 Codex 中启动完整项目

在项目目录中直接提出：

```text
使用 $run-modeling-project 初始化并完成这个数学建模项目。
使用 competition 档：逐题建立基线、验证并交付可审阅论文；
任何影响结论的验证失败都先返回对应阶段修复，不要绕过。
```

已有项目继续运行：

```text
使用 $run-modeling-project 检查当前状态和历史证据哈希，
从未完成的阶段继续，不要重做已经通过且证据未变化的阶段。
```

只执行一个阶段：

```text
使用 $analyze-model-data 审计这些 Excel 数据，保留末段时间留出。
```

```text
使用 $solve-model 按 decision_contract 求解，
同时生成基线、状态守恒、不确定性和权衡验证。
```

```text
使用 $write-model-paper 按本赛事的官方模板生成正式论文；
若是 CUMCM，再应用 CUMCM TeX 规则。
```

仅做可行性探索时：

```text
使用 $run-modeling-project 以 rapid 档分析这个题目。
保留题意、单位、约束、基线、随机种子和结果文件；
先不要生成正式论文，也不要声称结果已通过正式审查。
```

需要面向复现或外部审计的交付时：

```text
使用 $run-modeling-project 以 audit 档完成该项目。
执行完整 Skill 契约、运行清单、哈希复核和独立内容审查。
```

### 1.2 初始化可恢复项目状态

```powershell
python skills\run-modeling-project\scripts\project_state.py init `
  --project-root path\to\project `
  --project-id competition-2026-a `
  --manifest run-manifest.json
```

查看状态并检查历史证据：

```powershell
python skills\run-modeling-project\scripts\project_state.py status `
  --project-root path\to\project

python skills\run-modeling-project\scripts\project_state.py verify `
  --project-root path\to\project
```

`verify` 发现历史文件缺失或 SHA-256 改变时，后续阶段不得推进。

### 1.3 选择工作流档位

| 档位 | 适用情形 | 最小交付 | 不能声称 |
|---|---|---|---|
| `rapid` | 选题、可行性与早期迭代 | 题意/单位/约束、原始输入、基线、可复现参数或随机种子、结果和局限 | 已验证、可提交或正式复现 |
| `competition`（默认） | 竞赛论文与正式建议 | 逐题选型、适当的数据审计、基线与验证、可再生结果、读者可见的图表/论文和提交前审查 | 审计级正式验证 |
| `audit` | 高风险、可复用或外部审阅项目 | `competition` 全部内容，加完整契约、运行追踪、哈希当前证据与独立复核 | 无；任一审计门禁失败即不能通过 |

所有档位都禁止伪造数据、结果、引用或验证；原始数据、单位、约束、基线、随机性和影响决策的局限不可省略。CUMCM TeX/PDF/DOCX 规则仅在该赛事或明确等效的投稿要求下启用。

---

## 2. 系统架构

```text
题面与原始数据
      │
      ▼
select-model ──► decision_contract ──────────────────────────────┐
      │                                                         │
      ▼                                                         │
analyze-model-data ──► data_analysis / leakage_report            │
      │                                                         │
      ├──► research-model-literature ──► verified references     │
      │                                                         │
      ▼                                                         │
solve-model ──► result_object / quality_validation ──────────────┤
      │                                                         │
      ▼                                                         │
make-model-figures ──► figure_registry / figure audits           │
      │                                                         │
      ▼                                                         │
write-model-paper ──► Markdown / TeX / generation reports        │
      │                                                         │
      ▼                                                         │
review-model-paper ◄─────────────────────────────────────────────┘
      │
      ▼
PDF / DOCX 渲染检查与 release
```

三层职责：

| 层 | 位置 | 职责 |
|---|---|---|
| Skill 规则层 | `skills/*/SKILL.md` | 决定何时调用、必须输入什么、何时阻塞 |
| 通用算法层 | `code/algorithms/` | 数值计算、数据诊断、证据契约、图表与论文审查 |
| 工作流功能层 | `scripts/` | 执行契约、运行追踪、哈希、生产者、基准与旁路检查 |

题目字段、常数、具体表格列和题目专用图表只能放在项目适配器中，不得写进通用算法。

---

## 3. 8 个标准 Skills

| Skill | 主要职责 | 核心新增门禁 |
|---|---|---|
| `$run-modeling-project` | 阶段编排、断点恢复、证据交接 | 执行前角色预检；历史证据重新哈希 |
| `$select-model` | 逐题分解、候选模型、基线和决策契约 | 问题 ID 唯一；结果与验证产物不可缺失 |
| `$analyze-model-data` | 原始数据审计、EDA、泄漏和时间留出 | 面板稀疏性、时序/实体依赖和末段留出 |
| `$research-model-literature` | 检索、来源核验、方法对照和 BibTeX | 检索日志、抓取状态、元数据核验、相关性证据和 DOI 身份一致 |
| `$solve-model` | 建模、求解、仿真、基线和质量验证 | 状态守恒、同口径基线、服务水平和权衡证据 |
| `$make-model-figures` | 决策图、诊断图、正式导出和图形审计 | PDF/SVG/450 dpi PNG/`.figure.json` |
| `$write-model-paper` | 从注册证据生成 Markdown、TeX、PDF/DOCX 渲染请求 | 逐题覆盖、claim 绑定、严格 CUMCM 布局与渲染产物要求 |
| `$review-model-paper` | 六类失败即停审查和独立二次复核 | 正文、结果、注册表、正式 PDF/DOCX、渲染审计的哈希绑定 |

### 3.1 机器输入/输出契约

| Skill | 必需输入角色 | 必需输出角色 |
|---|---|---|
| `run-modeling-project` | `project_config`, `skill_trace` | `project_state`, `stage_gate`, `handoff` |
| `select-model` | `official_problem`, `data_dictionary`, `constraint_summary`, `project_goal` | `problem_decomposition`, `candidate_models`, `baseline_plan`, `selected_model_plan`, `decision_contract`, `risk_register`, `model_selection_report` |
| `analyze-model-data` | `raw_data`, `data_dictionary`, `official_problem` | `data_profile`, `data_quality_report`, `cleaning_actions`, `eda_findings`, `leakage_report`, `processed_data_manifest`, `data_analysis` |
| `research-model-literature` | `model_problem`, `selected_model`, `citation_requirements`, `source_candidates` | `search_queries`, `search_results`, `selected_sources`, `rejected_sources`, `retrieval_log`, `metadata_verification`, `relevance_evidence`, `literature_evidence`, `references_bib`, `bib_validation` |
| `solve-model` | `selected_model_plan`, `decision_contract`, `data_analysis`, `model_config`, `baseline_plan` | `model_specification`, `result_object`, `solver_validation`, `quality_validation` |
| `make-model-figures` | `result_object`, `quality_validation`, `claim_registry`, `figure_plan` | `figure_registry`, `figure_audit_report` |
| `write-model-paper` | `paper_spec`, `evidence_index`, `decision_contract`, `quality_validation`, `claim_registry`, `formula_registry`, `figure_registry`, `table_registry`, `references_bib`, `result_object` | `main_markdown`, `main_tex`, `paper_generation_report`, `claim_usage_report`, `cumcm_layout_validation`, `render_request` |
| `review-model-paper` | `manuscript`, `main_pdf`, `main_docx`, `official_problem`, `decision_contract`, `quality_validation`, `result_object`, `claim_registry`, `figure_registry`, `table_registry`, `references_bib`, `project_manifest`, 4 类渲染报告 | `paper_review_report`, `independent_content_review`, `review_hash_binding`, `submission_readiness` |

契约版本当前为 `1.3`。没有版本字段的历史运行按 `1.1` 解释，`1.2` 的历史运行仍按旧契约验证；新运行必须满足 `1.3`。同一 Skill 可以合法重跑：运行时保留全部签名历史，但只以该 Skill 的最新成功运行核验当前文件，避免“为通过校验而删除旧记录”。

---

## 4. 标准项目目录

推荐结构：

```text
project/
├── .modeling/
│   └── project-state.json
├── data/
│   ├── raw/                       # 不可覆盖
│   └── processed/
├── docs/
│   ├── problem-source.md
│   ├── data-dictionary.json
│   └── task-decomposition.md
├── results/
│   ├── decision_contract.json
│   ├── data_profile.json
│   ├── selected_model_plan.json
│   ├── model_specification.json
│   ├── result_object.json
│   ├── solver_validation.json
│   ├── quality_validation.json
│   └── simulation_metrics.json
├── figures/
│   ├── figure.pdf
│   ├── figure.svg
│   ├── figure.png
│   └── figure.figure.json
├── paper/
│   ├── paper-spec.yaml
│   ├── evidence-index.json
│   ├── claim-registry.json
│   ├── formula-registry.json
│   ├── figure-registry.json
│   ├── table-registry.json
│   ├── references.bib
│   ├── main.md
│   ├── main.tex
│   └── main.pdf
├── reports/
│   ├── data_analysis.md
│   ├── model_selection_report.md
│   ├── figure_audit_report.json
│   ├── independent_review.json
│   └── paper_review_report.json
├── manifests/
│   ├── skill-runs.jsonl
│   └── artifact-producers.json
└── run-manifest.json
```

所有正式证据路径必须相对项目根目录。原始数据、失败运行、随机种子和局限均需保留。

---

## 5. 完整工作流

### 5.1 题意拆解与模型选择

`$select-model` 必须输出逐题 `decision_contract`。最小结构：

```json
{
  "version": "1.2",
  "questions": [
    {
      "id": "Q1",
      "result_artifact": "results/q1-result.json",
      "validation_artifact": "results/q1-validation.json",
      "requires_dynamic_state": false,
      "requires_uncertainty": false,
      "requires_tradeoff": false,
      "requires_baseline": true
    }
  ]
}
```

复杂模型之前先定义同输入、同约束、同指标的简单基线。动态、不确定性和多目标语义必须在写代码前声明。

### 5.2 数据分析

对于实体×时间数据，除行列数、缺失和异常外，应调用：

```python
from algorithms import panel_diagnostics

diagnostics = panel_diagnostics(panel_values, holdout_periods=24)
```

输出包括：

- 零值比例与活跃实体比例；
- 中位滞后一阶相关；
- 实体间平均绝对相关；
- 训练段与末段留出的均值、零值比例；
- 无法估计的统计量以 `null` 表示。

未来规划不得只使用随机打乱切分。插补、缩放、编码和特征选择必须在切分后拟合。

### 5.3 文献调研

文献 Skill 不再内置任何具体赛题书目。候选来源需显式保存：

```json
[
  {
    "id": "method2024",
    "title": "Verified Method Title",
    "author": "A. Author",
    "year": 2024,
    "type": "article",
    "doi": "10.1000/example",
    "query": "method keyword",
    "purpose": "支持方法定义"
  }
]
```

每条记录必须有标题、年份、类型以及 DOI、ISBN、稳定 URL 中至少一项。重复 ID 或重复标识符会阻塞 BibTeX 生成。

优秀论文可用于学习：

- 章节结构与四问衔接；
- 公式和表格组织；
- 基线、灵敏度、稳健性与不确定性表达；
- 模型验证和局限讨论。

严禁复制其原文、具体公式表述、供应商名单、参数或数值答案。

### 5.4 建模与求解

当前通用质量接口：

```python
from algorithms import (
    compare_policy_metrics,
    lexicographic_order,
    service_level_metrics,
    validate_state_balance,
)
```

动态状态：

```python
audit = validate_state_balance(
    initial_state,
    inflows,
    outflows,
    observed_end_states=recorded_states,
    lower_bound=safety_floor,
)
```

服务水平：

```python
metrics = service_level_metrics(achieved, target)
```

必须同时报告均值、最小值、5% 分位和目标达成率，不能只给平均表现。

基线比较：

```python
comparison = compare_policy_metrics(
    baseline,
    candidate,
    shared_inputs=True,
    lower_is_better={"cost", "loss"},
)
```

`shared_inputs=False` 会直接拒绝比较，防止不同情景结果被包装成模型改进。

多目标问题应明确采用 Pareto、epsilon-constraint、词典序或权重敏感性。采用词典序时：

```python
ranked = lexicographic_order(
    policies,
    [("risk", "min"), ("service", "max"), ("cost", "min")],
)
```

### 5.5 运行清单

复制规格：

```powershell
Copy-Item schemas\run-manifest.example.json path\to\project\run-spec.json
```

创建和验证：

```powershell
python scripts\run_manifest.py create `
  --spec path\to\project\run-spec.json `
  --project-root path\to\project `
  --output path\to\project\run-manifest.json

python scripts\run_manifest.py validate `
  path\to\project\run-manifest.json `
  --project-root path\to\project `
  --verify-files
```

生成器登记输入/输出 SHA-256、字节数、Git 状态、Python/依赖版本、参数、种子、指标、失败尝试和局限。

---

## 6. 运行契约与证据门禁

### 6.1 标准执行器

每个 Skill 的 `scripts/execute_skill.py` 都调用统一执行器。示例：

```powershell
python skills\solve-model\scripts\execute_skill.py `
  --project-root path\to\project `
  --project-id competition-2026-a `
  --skill-path skills\solve-model `
  --input selected_model_plan=results/selected_model_plan.json `
  --input decision_contract=results/decision_contract.json `
  --input data_analysis=reports/data_analysis.md `
  --input model_config=results/model_config.json `
  --input baseline_plan=results/baseline_plan.json `
  --output model_specification=results/model_specification.json `
  --output result_object=results/result_object.json `
  --output solver_validation=results/solver_validation.json `
  --output quality_validation=results/quality_validation.json `
  -- python src\solve.py
```

输入或输出角色不完整时：

1. 运行被登记为 `BLOCKED`；
2. `src\solve.py` 不会执行；
3. 不会登记正式产物生产者。

成功记录包含运行时版本、契约版本、Skill 目录聚合实现哈希、输入/输出哈希和命令。

### 6.2 阶段门禁

| 进入阶段 | 必需证据角色 |
|---|---|
| `intake` | 无 |
| `analysis` | `problem_source`, `task_decomposition` |
| `modeling` | `data_audit`, `model_selection`, `decision_contract` |
| `validation` | `implementation`, `machine_results`, `quality_validation`, `run_manifest` |
| `writing` | `validation_report`, `quality_validation`, `paper_spec`, `evidence_index`, `claim_registry`, `figure_registry`, `table_registry`, `formula_registry` |
| `review` | `manuscript`, `decision_contract`, `quality_validation`, `figure_inventory`, `paper_review_report`, `run_manifest` |
| `release` | `review_report`, `submission_checklist`, `run_manifest` |

推进示例：

```powershell
python skills\run-modeling-project\scripts\project_state.py advance `
  --project-root path\to\project `
  --to analysis `
  --evidence problem_source=docs/problem-source.md `
  --evidence task_decomposition=docs/task-decomposition.md `
  --note "题面和子问题已核验"
```

每次推进前会重新检查以前所有门禁证据。不要修改文件后只更新登记哈希；应返回受影响阶段重新执行。

### 6.3 阻塞和恢复

```powershell
python skills\run-modeling-project\scripts\project_state.py block `
  --project-root path\to\project `
  --reason "缺少官方数据字典" `
  --next-action "补充字段单位和缺失值含义"

python skills\run-modeling-project\scripts\project_state.py resume `
  --project-root path\to\project
```

`resume` 只在原因解决后使用；阻塞历史不会删除。

### 6.4 全流程反旁路检查

```powershell
python scripts\validate_skill_workflow.py `
  --project-root path\to\project `
  --project-id competition-2026-a `
  --output path\to\project\reports\workflow-validation.json
```

检查内容：

- 8 个标准 Skills 是否存在成功运行；
- 追踪签名和输入/输出哈希是否有效；
- 正式 PDF、DOCX、编译报告与视觉审计是否由允许的生产者生成；
- review hash binding 是否仍覆盖当前正文、结果、注册表、文献和正式交付件；
- 项目脚本是否直接写 `main.tex`、直接编译或手写 Skill trace。

局部开发时可加 `--allow-partial-skills`，正式 release 不应使用。

---

## 7. 算法库

当前 `code/algorithms/` 有 25 个模块文件和 144 个公开导出。除既有算法外，`process_forecasting` 提供通用的时间顺序起点切分、因果窗口特征、未来事件标签、回归评分和保留集指标自助法；题目适配器不得把这些通用逻辑复制进项目脚本。

### 7.1 模块全景

| 类别 | 模块 | 主要能力 |
|---|---|---|
| 综合评价 | `ahp`, `fuzzy_math`, `evaluation` | AHP、模糊评价、熵权、TOPSIS、DEA、PCA、RSR、FAHP、灰色关联 |
| 预测回归 | `grey_system`, `regression`, `interpolation`, `time_series` | 灰色预测、回归、插值、移动平均、指数平滑、增长曲线 |
| 优化规划 | `math_programming`, `metaheuristic` | LP/IP/目标规划/NLP、GA、PSO、SA、ACO、人工鱼群 |
| 图论网络 | `graph_theory` | 最短路、Floyd、MST、最大流、最小费用流、关键路径、匹配 |
| 随机仿真 | `monte_carlo` | 积分、优化、通用仿真、排队、随机游走 |
| 机器学习 | `neural_network` | BP、RBF、SOM、MIV |
| 复杂系统 | `cellular_automata` | 生命游戏、森林火灾、DLA、SIRS、NaSch |
| 图像处理 | `image_processing` | 滤波、边缘、分割、形态学、直方图和特征 |
| 图表与结构图 | `sci_figures`, `diagram` | 出版图表、流程图、ER 图、系统图、学术表 |
| 论文质量 | `paper_check`, `paper_readiness`, `adversarial_review` | 结构、证据、注册表、语义和独立复核 |
| 数据诊断 | `data_diagnostics` | 面板稀疏性、依赖和时间留出 |
| 建模质量 | `modeling_quality` | 状态守恒、服务水平、基线比较、词典序 |
| 证据契约 | `modeling_contracts` | 决策、逐题产物、质量、文献与 BibTeX |
| 过程预测 | `process_forecasting` | 因果窗口特征、时间留出、事件标签和保留集评估 |
| 文档校验 | `document_validation` | 文档结构、版面与正式交付前检查 |
| 生产保护 | `production_guards` | 产物生产者、旁路和可追溯性保护 |

### 7.2 使用方法

从仓库根目录：

```python
import sys
from pathlib import Path

sys.path.insert(0, str(Path("code").resolve()))

from algorithms import (
    ahp_weight,
    entropy_weight,
    linear_programming,
    panel_diagnostics,
    topsis,
    validate_state_balance,
)
```

使用前以 `inspect.signature` 或源代码核对参数，不要从旧 README 猜测函数签名。

### 7.3 选择原则

- 先建立可运行基线，再使用复杂算法；
- 评价模型必须解释指标方向、归一化和权重来源；
- 优化器收敛不等于全局最优；
- 仿真必须保存随机种子和聚合统计；
- 预测必须使用时间顺序验证；
- 多目标结果必须呈现权衡；
- 动态模型必须逐期核对状态平衡。

---

## 8. 功能库

仓库级 `scripts/`：

| 脚本 | 用途 |
|---|---|
| `install_skills.py` | 安装标准 Skills |
| `validate_skills.py` | 校验 8 个 Skill 结构和元数据 |
| `skill_contracts.py` | 定义输入/输出角色和契约版本 |
| `run_standard_skill.py` | 执行前预检、调用命令并登记终态 |
| `skill_runtime.py` | 签名追踪、实现哈希和生产者登记 |
| `run_manifest.py` | 创建、验证统一运行清单 |
| `validate_skill_workflow.py` | 检查旁路、生产者和完整 Skill 链 |
| `check_coverage.py` | 执行覆盖率门槛 |
| `score_benchmark.py` | 评分单个历史题 scorecard |
| `validate_benchmarks.py` | 校验 12 个历史题基准 |
| `validate_legacy_archive.py` | 检查旧版资源是否正确隔离 |

职责区分：

- 功能库保证“是否按规则运行、是否留痕、产物是谁生成”；
- 算法库保证“如何计算和验证”；
- Skills 保证“什么时候调用、需要什么证据、何时必须阻塞”。

---

## 9. 图表生成

正式图表必须通过 `$make-model-figures` 和 `export_publication_figure`。

```python
import matplotlib as mpl
import matplotlib.pyplot as plt

from algorithms import (
    FigureContract,
    export_publication_figure,
    publication_rc_params,
    publication_size,
)

contract = FigureContract(
    claim="候选方案在同一压力情景下提高服务水平",
    evidence=("候选与基线服务水平",),
    source_paths=("results/policy-comparison.csv",),
    column="double",
    figure_role="model-comparison",
    scenario="相同末段历史压力窗口",
    randomness="确定性评估",
    n_definition="24 个周周期",
    statistic="逐周服务水平",
)

with mpl.rc_context(publication_rc_params()):
    fig, ax = plt.subplots(figsize=publication_size("double", 82))
    ax.plot(weeks, baseline, label="baseline")
    ax.plot(weeks, candidate, label="candidate")
    ax.set(xlabel="Week", ylabel="Service level")
    ax.legend()
    export_publication_figure(
        fig,
        "figures/policy_comparison",
        contract,
        dpi=450,
        strict=True,
        close=True,
    )
```

一次输出：

| 文件 | 用途 |
|---|---|
| `.pdf` | 矢量论文主稿 |
| `.svg` | 可编辑矢量版本 |
| `.png` | 450 dpi 渲染校样 |
| `.figure.json` | claim、数据源、尺寸、字体、布局和导出审计 |

禁止项目适配器另写 240 dpi 或手工 `savefig` 旁路。每个正式图的 registry 条目应包含数据源、元数据文件、哈希、样本定义和正文插入状态。

---

## 10. 论文生成与格式交付

### 10.1 正式论文前置文件

版式的唯一权威是 `skills/write-model-paper/assets/cume-template.tex`；`config/paper_style.yaml` 记录同一套默认值。本文只说明入口和校验，不得据此另写 HTML、网页预览或第二套 TeX 版式。CUMCM 风格预览必须实际生成 PDF 并渲染页面：

```powershell
python skills\write-model-paper\scripts\render_template_preview.py `
  --output-dir output\pdf
```

该命令会生成 `cume-template-preview.pdf` 及每页 PNG 校样。缺少 XeLaTeX 或无法输出 PDF 时应报告失败，不能用 HTML、浏览器截图或教学提示条替代。

`competition_paper` 至少需要：

```text
paper-spec.yaml
evidence-index.json
claim-registry.json
formula-registry.json
figure-registry.json
table-registry.json
references.bib
results/decision_contract.json
results/quality_validation.json
results/result_object.json
```

关键数字使用：

```text
[claim:CLAIM_ID]
```

并通过 `claim-registry.json` 解析到证据文件、JSON Pointer 和 SHA-256。

### 10.2 内容门槛

可在 `paper-spec.yaml` 中声明：

```yaml
mode: competition_paper
minimum_body_characters: 9000
minimum_equations: 10
minimum_figures: 8
minimum_tables: 6
minimum_references: 8
```

这些数字只是交付下限，不是优秀论文的充分条件。无论数量多少，每个问题仍必须具有：

- 可执行结果文件；
- 独立验证文件；
- 决策变量和模型关系；
- 适用的状态/可行性、基线/权衡或不确定性讨论；
- 局限和失效条件。

### 10.3 CUMCM 专用 TeX 规则

仅当目标赛事为 CUMCM 或明确采用同等规则时使用。权威模板：

```text
skills/write-model-paper/assets/cume-template.tex
```

要求：

- A4；
- 左右页边距 `2.5 cm`，上 `2.5 cm`、下 `2.8 cm`；
- 正文宋体，标题黑体；
- 正文行距 `1.65`，首行缩进 `2em`；
- 默认首页包含题目、摘要和关键词，不强制封面；
- 目录位于摘要之后，正文页码从 1 开始；
- 正文页码重新从 1 开始；
- 一级标题居中 16pt 黑体；
- 二、三级标题按模板层级；
- 中文图表标题；
- booktabs 三线表；
- 固定浮动体间距；
- 上标引用、参考文献标题和代码附录环境；
- 附录代码框使用左侧行号（`numbers=left`）、`6.5pt` 灰色行号与 `7pt` 间距。

生成后运行：

```powershell
python skills\write-model-paper\scripts\validate_cumcm_layout.py `
  --tex path\to\project\paper\main.tex
```

输出不是 `PASS strict_cumcm_layout` 时不得编译为正式论文。禁止用 `\maketitle`、通用 LaTeX class 或 PDF 后处理替代模板规则。

### 10.4 正式 PDF 与 DOCX 双交付

PDF 是版式权威：用 XeLaTeX 至少编译两次，记录页数、A4 尺寸、源 TeX 哈希和任何警告。DOCX 是可编辑伴随件：Pandoc 转换时将 PDF 图映射到登记的 450 dpi PNG，再以 `format_cumcm_docx.py` 统一宋体/黑体、页边距和三线表；随后以 Word 或可用的标准渲染器导出 PDF，逐页审计。

DOCX 默认应与权威 TeX 的首页结构一致：题目、摘要和关键词在首页，随后是目录，正文从 1 开始编号。只有赛事规则明确要求时才添加封面。若某个环境无法实现，不能静默降级；必须在 `docx_render_report.json` 中标明阻塞或差异，审查 Skill 会拒绝将其伪装为正式合格交付。

### 10.5 PDF 与 DOCX

- TeX/Markdown 由 `$write-model-paper` 正式运行生成；
- PDF 应由正式 PDF/LaTeX 工具生成并渲染检查；
- DOCX 应由正式文档工具生成并逐页检查；
- 项目脚本不得直接写 `main.tex` 或绕过生产者登记；
- PDF/DOCX 的内容必须来自同一组已验证证据。

---

## 11. 论文审查

`$review-model-paper` 执行六个失败即停门禁：

| 门禁 | 检查内容 |
|---|---|
| structure | 模式、章节、篇幅和逐题覆盖 |
| mathematics | 公式、变量、目标、约束、状态平衡 |
| figures_tables | 文件存在、正文插入、registry 与审计 |
| evidence | claim、JSON Pointer、SHA-256、逐题产物 |
| validation | seed、聚合统计、基线、权衡、不确定性和失败状态 |
| citations | 来源身份、正文使用、DOI/ISBN/URL |

Markdown 审查：

```powershell
python skills\review-model-paper\scripts\run_paper_check.py `
  path\to\project\paper\main.md `
  --project-root path\to\project `
  --mode competition_paper `
  --report-dir path\to\project\reports
```

复杂论文自动审查没有发现语义问题时，必须进行独立二次复核：

```powershell
python skills\review-model-paper\scripts\second_pass_content_review.py `
  --project-root path\to\project `
  --manuscript path\to\project\paper\main.md `
  --decision-contract path\to\project\results\decision_contract.json `
  --quality-validation path\to\project\results\quality_validation.json
```

独立复核同时绑定：

1. manuscript SHA-256；
2. decision contract SHA-256；
3. quality validation SHA-256。

任何一项变化后旧复核自动失效。复核还检查局限披露、逐题产物存在、逐题正文覆盖、动态/不确定性/权衡/基线讨论和质量文件结构。

审查失败应返回 `modeling`、`validation` 或 `writing` 修复，不允许修改报告状态或跳到 release。

---

## 12. 测试与基准

### 12.1 全量测试

```powershell
python -m pytest code\tests -q
```

当前提交基准：**351 passed**（2026-08-10；含 12 个语法旁路变体和 12 个语义/完整性 mutation 场景）。pytest 已固定使用仓库内、Git 忽略的 `.test-tmp/pytest`，避免 Windows 上访问受限的系统临时目录。

mutation 覆盖直接写论文、拼接/编码绕过、删 trace、篡改结果、伪造 DOI、换图源、缺独立复核、复核后改 claim、超大方案未谈可执行性，以及题目适配器吸收编排等。完整报告应写入项目 `reports/mutation-tests.json`，而不是只在终端口头声明。

### 12.2 Skill 校验

```powershell
$env:PYTHONUTF8 = "1"
python scripts\validate_skills.py
```

预期：

```text
Validated 8 Codex Skills under skills
```

Windows 中文环境下给 `PYTHONUTF8=1` 可避免第三方 Skill 校验器使用 GBK 读取 UTF-8 文档失败。

### 12.3 历史题基准

`benchmarks/` 收录 2020—2023 年 CUMCM A/B/C 共 12 道题的模型族、最低基线、验证目标和常见失败模式，不复制完整题面。

```powershell
python scripts\validate_benchmarks.py
```

预期：

```text
Validated 12 historical benchmark cases
```

### 12.4 覆盖率

```powershell
python -m pytest code\tests --cov=algorithms --cov-report=json:coverage.json
python scripts\check_coverage.py coverage.json
```

`coverage-policy.json` 定义总体及关键模块门槛。测试覆盖正常样例、异常输入、边界条件、数学性质、固定 seed、差分验证和对抗旁路。

---

## 13. 2021 C 题适配器边界

仓库保留以下 CUMCM 2021 C 适配器，用于真实迁移和回归：

```text
skills/select-model/scripts/plan_problem.py
skills/analyze-model-data/scripts/analyze_excel_inputs.py
skills/solve-model/scripts/solve_supplier_chain.py
skills/make-model-figures/scripts/render_supplier_figures.py
skills/write-model-paper/scripts/write_supplier_competition_paper.py
```

适配器允许包含：

- 题目四问映射；
- 供应商/转运商 Excel 字段；
- A/B/C 原料转换关系；
- 24 周计划；
- 本题表格和图形列映射。

必须复用通用层的部分：

- 决策契约校验；
- 面板时间诊断；
- 状态守恒；
- 服务水平；
- 同口径基线；
- 词典序排序；
- 正式图形导出；
- 论文证据与独立复核。

禁止：

- 将 C 题固定书目写进文献 Skill；
- 将供应商名单或计算结果写进通用算法；
- 为接近优秀论文答案调整参数；
- 用题目脚本绕过 8 个标准 Skills；
- 将压力测试描述成已校准概率保证。

本次官方优秀论文学习报告位于：

```text
benchmarks/learning-reports/cumcm-2021-c-official-showcase-study.md
```

Skill、算法库、功能库和论文能力总览位于：

```text
reports/skill-capability-overview.md
```

### 13.1 第二道真实迁移验证

`C题：化工厂生产流程的预测和控制问题` 只使用其官方 DOCX 和两张原始 CSV，从零运行 8 个 Skills。它验证了供应链题专用代码没有被伪装成通用能力：通用层负责因果时间特征、顺序切分、事件标签和误差统计，项目适配器只保留五输入/双输出、30 步历史与 `t+10..t+70` 窗口。迁移项目必须保留失败运行、`run-manifest.json`、PDF/DOCX 渲染报告、`review_hash_binding.json` 和 `mutation-tests.json`。

---

## 14. 常见问题

### 14.1 为什么命令没有执行，而记录是 BLOCKED？

标准执行器会在启动子进程前检查全部输入/输出角色。补齐缺失角色后重新运行，不要手工改 trace。

### 14.2 为什么以前通过的阶段现在不能推进？

`project_state.py verify` 发现历史证据缺失或哈希变化。返回最早受影响阶段，重新生成后续产物。

### 14.3 为什么 bibliography 生成失败？

检查 `source_candidates`：

- 是否为 JSON 数组；
- 每项是否有唯一 `id`；
- 是否有 `title`、`year`、`type`；
- 是否至少有 DOI、ISBN、URL 之一；
- 是否存在重复标识符。
- 是否记录 `origin`、检索提供者、带时区时间戳和 `FETCHED` 状态；
- DOI 是否与 `metadata_verification.verified_identifier` 一致；
- 是否明确说明该来源支持什么、证据位置在哪里。

### 14.4 为什么图已经存在仍不能进入论文？

仅有 PNG 不足以作为正式图。需要 PDF、SVG、450 dpi PNG、`.figure.json`、figure registry、数据源和正文插入状态。

### 14.5 为什么论文图表和公式数量足够仍然 FAIL？

数量只是下限。逐题结果或验证缺失、claim 无法解析、历史证据过期、模型质量要求未讨论，都会阻塞。

### 14.6 为什么独立审查突然失效？

正文、决策契约或质量验证有一项变化，原三哈希报告即失效。重新运行二次复核。

### 14.7 能否直接使用旧 `/mm-*` 命令？

可作为历史材料阅读，但新项目应调用对应标准 Skill。旧命令没有当前的契约、实现哈希、生产者和六门禁保证。

### 14.8 这些门禁能保证获奖吗？

不能。它们保证流程、证据、格式和可复现性达到仓库标准，不保证竞赛名次。模型洞察、数据质量和真实创新仍需独立判断。

---

## 15. 旧版资源说明

旧资源路径：

| 路径 | 状态 |
|---|---|
| `legacy/skills/` | 只读兼容档案 |
| `legacy/command-map.yaml` | 旧命令到标准 Skill 的迁移映射 |
| `patterns/` | 可迁移写作和图表规则参考 |
| `template/cume-template.tex` | 旧模板位置；正式写作以 Skill asset 为准 |
| `~/ai-skills/paper-skills/` | 可选外部旧资源，不是当前依赖 |
| `~/ai-skills/math-skills/` | 可选外部旧资源，不是当前依赖 |

常见迁移：

| 旧用途 | 当前入口 |
|---|---|
| `/mm-analyze`, `/mm-route` | `$select-model` |
| `/data-analysis`, `/mm-data` | `$analyze-model-data` |
| `/mm-literature`, `/nature-search` | `$research-model-literature` |
| `/mm-model`, `/mm-solve`, `/mm-or` | `$solve-model` |
| `/mm-visualize`, `/mm-diagram`, `/paper-figures` | `$make-model-figures` |
| `/mm-paper`, `/paper-writing` | `$write-model-paper` |
| `/mm-review`, `/proofread`, `/validate-bib` | `$review-model-paper` |
| `/mm-workflow` | `$run-modeling-project` |

维护原则：

1. 新能力优先加入通用算法、功能脚本或 8 个标准 Skills。
2. 题目专用规则只进入项目适配器。
3. 旧命令文档不再作为当前行为依据。
4. 每次升级同时增加对抗测试、更新本说明并运行全量验证。
